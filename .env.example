# Backend origin. Defaults to http://localhost:4000 (http://10.0.2.2:4000 on the
# Android emulator). Only needed for a physical device over Expo Go — point it at
# your dev machine's LAN IP, e.g. http://192.168.1.23:4000.
EXPO_PUBLIC_API_BASE_URL=
